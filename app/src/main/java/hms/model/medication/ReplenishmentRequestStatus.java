package hms.model.medication;

/** Enumerates the possible statuses of a replenishment request. */
public enum ReplenishmentRequestStatus {

    /** Indicates that the replenishment request is awaiting approval. */
    PENDING,

    /** Indicates that the replenishment request has been approved. */
    APPROVED
}
